package corrupt

model Uniform {
  random PerfectPhylo phylo
  
  laws {
    logf(phylo) {
      - (phylo.nLoci + phylo.nCells - 2) * log(phylo.nLoci)
    }
  }
  
  generate (rand) {
    phylo.sampleUniform(rand.generator) 
  }
}