package corrupt

model Uniform {
  random PerfectPhylo phylo
  
  laws {
    logf(phylo) {
      - (phylo.nLoci + phylo.nCells - 1) * log(phylo.nLoci + 1)
    }
  }
  
  generate (rand) {
    phylo.sampleUniform(rand.generator) 
  }
}