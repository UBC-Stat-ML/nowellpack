package corrupt

import static corrupt.CorruptUtils.*

model Uniform {
  random PerfectPhylo phylo
  
  laws {
    logf(phylo) {
      - logNPerfectPhylo(phylo.cells.size, phylo.loci.size)
    }
  }
  
  generate (rand) {
    phylo.sampleUniform(rand.generator) 
  }
}