package corrupt

import static corrupt.CorruptUtils.*

model Uniform {
  random PerfectPhylo phylo
  
  laws {
    logf(phylo) {
      - logNPerfectPhylo(phylo.nCells, phylo.nLoci)
    }
  }
  
  generate (rand) {
    // TODO: could create one based on conifer
    throw new RuntimeException
  }
}